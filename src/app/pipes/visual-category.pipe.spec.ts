import { VisualCategoryPipe } from './visual-category.pipe';

describe('VisualCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new VisualCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
